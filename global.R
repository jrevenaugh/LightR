# Plotting parameters ----------------------------------------------------------
winningColor <- "deepskyblue1"
backgroundColor <- "gray30"
hintColor = "goldenrod1"

# Difficulty scale -------------------------------------------------------------
diffScale <- c(0.1, 0.25, 0.75, 0.5)

nSolution <- 15
nCurrent <- 0
